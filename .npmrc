# eslint-plugin-react's peer range still tops out at eslint 9, while this
# project is on eslint 10. The combination works; only the declared range is
# stale. Set here rather than passed per-command so tools that shell out to
# npm (the shadcn CLI, most notably) inherit it instead of failing to install.
legacy-peer-deps=true
